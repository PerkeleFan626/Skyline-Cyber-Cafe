#pragma once

#include <string>

using namespace std;

namespace filePath {
    inline const string DATA_DIR = "../csv_files/";

    inline const string PUBLIC_USER_FILE = "public_users.csv";
    inline const string ADMIN_FILE = "admins.csv";
    inline const string SESSION_FILE = "user_sessions.csv";
}